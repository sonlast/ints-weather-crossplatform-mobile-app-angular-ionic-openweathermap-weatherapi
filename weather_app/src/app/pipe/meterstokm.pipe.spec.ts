import { MeterstokmPipe } from './meterstokm.pipe';

describe('MeterstokmPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterstokmPipe();
    expect(pipe).toBeTruthy();
  });
});
